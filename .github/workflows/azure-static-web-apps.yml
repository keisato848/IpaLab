name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    permissions:
       id-token: write
       contents: read
    env:
      # Vercel, a similar service, sets this to true during build.
      # We can use it to gate functionality that should only run in production.
      CI: "true"

      # Keys and Connection Strings
      COSMOS_DB_CONNECTION: ${{ secrets.COSMOS_DB_CONNECTION }}
      APPLICATIONINSIGHTS_CONNECTION_STRING: ${{ secrets.APPLICATIONINSIGHTS_CONNECTION_STRING }}
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      # Custom Flags
      START_APP_INSIGHTS: "true" # Enable App Insights in runtime

      # NextAuth Configuration
      AUTH_SECRET: ${{ secrets.AUTH_SECRET }}
      AUTH_TRUST_HOST: "true" # Required for some environments

      # Auth Providers (GitHub, Google)
      AUTH_GITHUB_ID: ${{ secrets.AUTH_GITHUB_ID }}
      AUTH_GITHUB_SECRET: ${{ secrets.AUTH_GITHUB_SECRET }}
      AUTH_GOOGLE_ID: ${{ secrets.AUTH_GOOGLE_ID }}
      AUTH_GOOGLE_SECRET: ${{ secrets.AUTH_GOOGLE_SECRET }}
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
          persist-credentials: false

      - name: Install Dependencies
        run: npm ci

      - name: Build
        run: npx turbo run build --filter=web
        env:
          CI: "true"
          # Build-time secrets needed for static generation (if any) or config
          NEXT_PUBLIC_API_BASE: ${{ secrets.NEXT_PUBLIC_API_BASE }}

      - name: Cleanup Build Artifacts
        run: |
          rm -rf apps/web/.next/cache
        shell: bash
        # Note: Do NOT delete .next/standalone - it's required for Next.js server runtime

      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "apps/web" # Next.js app location for standalone detection
          api_location: "" # We use Next.js API routes, so no separate API folder
          output_location: ".next" # Next.js build output (relative to app_location)
          skip_app_build: true # Skip Azure build - we pre-build with secrets above
          skip_api_build: true # Skip API build - Next.js API routes are bundled in .next


  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          action: "close"
          app_location: "/" # Required by some validator versions even for close action
