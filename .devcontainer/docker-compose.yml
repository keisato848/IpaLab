# devcontainer 用 workspace サービス。
# Langfuse / OTel Collector は npm run otel:compose で必要時のみ手動起動する。
services:
  workspace:
    build:
      context: ../.devcontainer
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
