import { Text, View } from 'react-native';

/** dashboard 画面（WP-3/WP-4で実装） */
export default function Screen() {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>dashboard</Text>
        </View>
    );
}
